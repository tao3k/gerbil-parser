id = "rowan-engine-selective-glr-scale-v1"
title = "Generated selective-GLR parser-engine execution stays inside the Rowan latency red zone"
policy_ids = ["RUST-AGENT-ROWAN-ENGINE-PERFORMANCE-001"]
agent_goal = "Keep production-scale generated selective-GLR execution and lossless Rowan CST construction bounded."
reference_repositories = ["rust-analyzer/rowan", "tree-sitter/tree-sitter"]
reference_patterns = [
  "Scale benchmarks execute every admitted fixture in one bounded engine sample",
  "Selective-GLR work remains explicit in structural metrics",
]
inputs = "inputs"
expected = "expected"
